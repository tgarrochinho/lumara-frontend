/**
 * Test utility to verify path aliases work correctly
 */
export const testPathAlias = () => {
  return 'Path aliases work!'
}

export const greet = (name: string): string => {
  return `Hello, ${name}!`
}
